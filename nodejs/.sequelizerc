const { resolve } = require("path");

module.exports = {
    config: resolve(__dirname, "src", "config", "database.js"), // compatibiliza o formato de dir com outros sistemas operacionais
    "models-path": resolve(__dirname, "src", "models"),
    "migrations-path": resolve(__dirname, "src", "migrations"),
    "seeders-path": resolve(__dirname, "src", "seeds"), // sementes iniciais pra tabela // admin admin
}
